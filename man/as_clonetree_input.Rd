% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{as_clonetree_input}
\alias{as_clonetree_input}
\title{Convert clustering results to CloneTree's internal format}
\usage{
as_clonetree_input(
  data,
  subject = NULL,
  sample_col = "sampleID",
  cluster_col = "cluster.no",
  ccf_col = "CCF",
  mutation_col = "no.of.mutations",
  subject_col = "subject",
  clone_col = "Clone",
  trunk_value = "Y",
  cluster_prefix = "C",
  ccf_scale = c("auto", "fraction", "percent"),
  snap_ccf = TRUE,
  ccf_floor = 0.05,
  ccf_ceiling = 0.95,
  trunk_threshold = 0.95,
  summarize_ccf = c("max", "mean")
)
}
\arguments{
\item{data}{A data frame with one row per mutation cluster and sample.}

\item{subject}{Optional subject/tumor identifier to select when \code{data}
contains more than one tumor.}

\item{sample_col, cluster_col, ccf_col}{Column names for sample ID, cluster
ID, and CCF.}

\item{mutation_col}{Column with the number of substitutions/mutations in a
cluster. If absent, each cluster is assigned length 1.}

\item{subject_col}{Optional subject ID column.}

\item{clone_col}{Optional column marking trunk clusters.}

\item{trunk_value}{Value in \code{clone_col} that marks a trunk cluster.}

\item{cluster_prefix}{Prefix added to purely numeric cluster IDs. Use \code{NULL}
to keep cluster IDs unchanged.}

\item{ccf_scale}{One of \code{"auto"}, \code{"fraction"}, or \code{"percent"}.}

\item{snap_ccf}{Logical; if \code{TRUE}, values below \code{ccf_floor} become 0 and
values above \code{ccf_ceiling} become 1.}

\item{ccf_floor, ccf_ceiling}{Thresholds used when \code{snap_ccf = TRUE}.}

\item{trunk_threshold}{When no \code{clone_col} is available, clusters with CCF
at least this value in all samples are marked as trunk.}

\item{summarize_ccf}{How duplicate cluster/sample rows are summarized.}
}
\value{
A \code{clonetree_input} object.
}
\description{
Standardizes a long table of cancer cell fraction (CCF) estimates into the
matrix and metadata representation used by all CloneTree functions.
}
