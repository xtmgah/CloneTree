% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{clone_qc}
\alias{clone_qc}
\title{Check clone clusters against pigeonhole and crossing rules}
\usage{
clone_qc(
  data,
  subject = NULL,
  remove_clusters = NULL,
  presence_threshold = 0.02,
  ccf_tolerance = 0.03,
  crossing_tolerance = ccf_tolerance,
  objective = c("max_mutations", "max_clusters"),
  max_solutions = 1000,
  lock_trunk = TRUE,
  locked_clusters = NULL,
  ...
)
}
\arguments{
\item{data}{A clustering result table or a \code{clonetree_input} object.}

\item{subject}{Optional tumor/subject ID.}

\item{remove_clusters}{Optional cluster IDs to remove before QC. IDs can be
either original IDs (for example \code{"11"}) or prefixed IDs (for example
\code{"C11"}).}

\item{presence_threshold}{Clusters with maximum CCF below this value are
treated as absent.}

\item{ccf_tolerance}{Tolerance for the pigeonhole rule. Two independent
clusters are incompatible with separate branches if their summed CCF is
greater than \code{1 + ccf_tolerance} in any sample.}

\item{crossing_tolerance}{Tolerance for the crossing rule. Defaults to
\code{ccf_tolerance}.}

\item{objective}{How to choose among possible exclusions. \code{"max_mutations"}
keeps compatible solutions with the largest retained mutation count;
\code{"max_clusters"} keeps the largest number of clusters.}

\item{max_solutions}{Maximum number of equally best compatible solutions to
keep.}

\item{lock_trunk}{If \code{TRUE}, trunk clusters are never excluded by the solver.}

\item{locked_clusters}{Additional clusters that must be retained.}

\item{...}{Passed to \code{\link[=as_clonetree_input]{as_clonetree_input()}} when \code{data} is a raw data frame.}
}
\value{
A \code{clonetree_qc} object containing the standardized input, pairwise
rule table, and all retained-cluster solutions.
}
\description{
\code{clone_qc()} evaluates pairwise CCF relationships between mutation clusters.
A pair is considered incompatible when the two clusters cross across samples
and also violate the pigeonhole rule in at least one sample. In that case the
two clusters cannot both be kept under the usual evolutionary assumptions, so
CloneTree enumerates all best-scoring compatible retained-cluster sets.
}
