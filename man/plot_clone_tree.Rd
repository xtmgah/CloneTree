% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_clone_tree}
\alias{plot_clone_tree}
\title{Plot a publication-style clonal phylogenetic tree}
\usage{
plot_clone_tree(
  x,
  solution = 1,
  events = NULL,
  event_cluster_col = "cluster.no",
  event_label_col = NULL,
  event_subject_col = "subject",
  title = NULL,
  palette = clonetree_palette(),
  branch_width = 3.2,
  node_size = 4.4,
  branch_length = c("linear", "sqrt"),
  min_branch_fraction = 0.08,
  angle_width = 70,
  show_scale = TRUE,
  label_samples = TRUE,
  label_events = TRUE,
  filename = NULL,
  width = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{clonetree}, \code{clonetree_qc}, \code{clonetree_input}, or raw clustering
data frame.}

\item{solution}{Which QC solution to draw when \code{x} is not already a tree.}

\item{events}{Optional data frame of driver mutations or copy-number events
to mark on branches.}

\item{event_cluster_col, event_label_col, event_subject_col}{Column names in
\code{events}. If \code{event_label_col} is \code{NULL}, CloneTree looks for \code{info},
\code{event}, \code{label}, \code{driver}, \code{alteration}, or \code{annotation}.}

\item{title}{Plot title. Defaults to the subject ID when available.}

\item{palette}{Cluster color palette.}

\item{branch_width, node_size}{Line width and node size.}

\item{branch_length}{\code{"linear"} or \code{"sqrt"} visual branch-length transform.}

\item{min_branch_fraction}{Minimum visual branch length as a fraction of the
longest branch.}

\item{angle_width}{Fan width below the trunk. The default keeps first-level
branches within about 35 degrees of the vertical trunk.}

\item{show_scale, label_samples, label_events}{Logical toggles.}

\item{filename}{Optional output path. When supplied, the plot is saved and
returned invisibly.}

\item{width, height}{Optional output dimensions for \code{filename}.}

\item{...}{Passed to \code{\link[=infer_clone_tree]{infer_clone_tree()}} when needed.}
}
\value{
A \code{ggplot} object, or invisibly the filename if saved.
}
\description{
Plot a publication-style clonal phylogenetic tree
}
